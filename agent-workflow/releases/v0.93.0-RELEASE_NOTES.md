# GitHub Release notes — v0.93.0

| Field | Value |
|---|---|
| Tag | `v0.93.0` |
| Target | `e102a0d` |
| Title | `v0.93.0 — Collapsed shell default + sticky Support form header` |
| Change class | **Minor** |
| Prior production | `0.92.0` @ `9d95501` |
| Environment | GitHub Pages (`github-pages`) via `.github/workflows/deploy.yml` |

---

## Summary

Shell starts with sidebar and topbar collapsed by default, and the Support Activities Add/Edit card header stays sticky at the top while scrolling (with sticky top padding when the topbar is hidden).

## Features / UX

- Default sidebar state `collapsed` when no `sessionStorage` preference (`pt.sidebar.state`); expand/collapse still persists for the session
- Support form card `.ant-card-head` sticky using `--app-sticky-top` / `--app-sticky-top-pad`

## Security / configuration changes

- None (frontend UX only)
- Migrations: none

**Risk:** Low. Rollback: redeploy `0.92.0` @ `9d95501`.

## Testing / validation

| Check | Status | Result |
|---|---|---|
| `npm run typecheck` | PASSED | clean |
| `npm run build` | PASSED | Vite OK |
| GitHub Actions Deploy | PASSED | [29684541973](https://github.com/carlolidres/ProjectTracker_React/actions/runs/29684541973) |
| Browser smoke | NOT_RUN | owner |

## Paste-ready GitHub Release body

```markdown
## v0.93.0 — Collapsed shell default + sticky Support form header

**Environment:** GitHub Pages  
**Prior:** 0.92.0 @ 9d95501

### Highlights
- Sidebar and topbar start **collapsed** by default (session preference still honored)
- Support **Add / Edit Activity** header stays sticky while scrolling

### Migrations
- none
```
