# GitHub Release notes — v0.91.0

| Field | Value |
|---|---|
| Tag | `v0.91.0` |
| Target | _(fill after Actions)_ |
| Title | `v0.91.0 — Projects Database UX + TSD/QC remarks + About history` |
| Change class | **Minor** |
| Prior production | `0.90.0` @ `de385ef` |
| Environment | GitHub Pages (`github-pages`) via `.github/workflows/deploy.yml` |

---

## Summary

Projects Database spreadsheet UX improvements, Project Entry role-default tabs and database open expand, TSD/QC remarks columns, dashboard workspace Phase B create-return and sticky drill context, retired dead CNF create modal, and About drawer version history.

## Features added

- Projects Database: Full View default, content-sized SO No., FG Month sort/filter, Activity Type searchable creatable cells, Esc clears cell selection, Add project bar
- Project Entry: default role tab by signed-in role (admin → AM/BM/PL); expand Batch/MO/PO when opened from Projects Database (`from=database`)
- TSD `tsd_remarks` and QC `qc_remarks` long-text fields (migrations `20260718140000`, `20260718141000`)
- Spreadsheet hides long-text columns (edit on Project Entry)
- Dashboard workspace Phase B: `?new=1` + `return_to` create flows; sticky Dashboard context banner on Projects Database drills
- About drawer: curated version history (`src/lib/appVersionHistory.ts`)
- Single New CNF path via `CnfTrackerDetailModal` (removed unused `CnfCreateModal`)

## Defects fixed

- PLANNING group header renamed to PP
- Role tabs / Projects DB polish from Phase A follow-ups

## Security / configuration changes

- Optional `VITE_FEATURE_DASHBOARD_WORKSPACE` unchanged (default on)
- New columns on `cnf_projects` — apply migrations on target Supabase before relying on Remarks persistence

**Risk:** Low–Medium. Rollback: redeploy `0.90.0` @ `de385ef`. Remarks downs available if schema must reverse.

## Deferred

- Phase C R9 Support CNF/Endorsement handoff overlays (still navigate; drafts preserve state)

## Testing / validation

| Check | Status | Result |
|---|---|---|
| `npm run typecheck` | ☐ | |
| `npm run build` | ☐ | |
| `npm run test:dashboard-drilldown` | ☐ | |
| `npm run test:projects-db-validation` | ☐ | |
| GitHub Actions Deploy | ☐ | |
| Browser smoke | NOT_RUN | owner |

## Paste-ready GitHub Release body

```markdown
## v0.91.0 — Projects Database UX + TSD/QC remarks + About history

**Environment:** GitHub Pages  
**Package:** 0.91.0  
**Prior:** 0.90.0 @ de385ef  

### Highlights
- Projects Database Full View default, FG Month filters, Activity Type creatable cells, Esc clear selection
- Project Entry role-default tabs; expand hierarchy from Projects Database
- TSD/QC Remarks columns (migrations required on target)
- Spreadsheet omits long-text columns
- About drawer version history
- Retire dead CnfCreateModal; New CNF → detail modal only

### Migrations
- `20260718140000_cnf_projects_tsd_remarks`
- `20260718141000_cnf_projects_qc_remarks`

### Rollback
Redeploy `0.90.0` @ `de385ef`.
```
